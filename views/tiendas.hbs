<div class="container">
    <div class="row col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card my-2">
            <div class="card-body">
                <h2 class="card-title mb-5 text-center">Tiendas </h2>
                <table class="table table-striped table-hover text-center table-bordered ">
                    <thead>
                        <tr>
                            <th>Tienda</th>
                            <!-- <th>Sucursal</th> -->
                            <th>Direccion</th>
                            <th>Ciudad</th>
                            <th>Region</th>
                            <th>Estado</th>
                            <th>{{rol}}</th>
                        </tr>
                    </thead>
                    {{#each tiendas}}
                    <tr>
                        <td>{{this.nombre}}</td>
                        <!-- <td>{{this.sucursal}}</td> -->
                        <td>{{this.direccion}}</td>
                        <td>{{this.ciudad}}</td>
                        <td>{{this.region}}</td>
                        {{#if this.estado}}
                            <td><p style="color: green;">Activo</p></td>
                        {{else}}
                            <td><p style="color: red;">Pendiente</p></td>
                        {{/if}}
                    </tr>
                    {{/each}}
                </table>
                <a href="/control/agregarT" class="btn btn-primary w-100 mt-4">Agregar Tienda</a>
                <a href="/control/main" class="btn btn-danger w-100 mt-3">Volver</a>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.4.10/dist/sweetalert2.all.min.js"></script>
    <script src="javascript/script.js"></script>
</div>