<div class="container">
    <div class="row col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card my-2">
            <div class="card-body">
                <h2 class="card-title mb-5 text-center">Tiendas </h2>
                <table class="table table-stripped text-center">
                    <thead>
                        <tr>
                            <th>Tienda</th>
                            <!-- <th>Sucursal</th> -->
                            <th>Direccion</th>
                            <th>Ciudad</th>
                            <th>Region</th>
                            <th>Estado</th>
                            <th colspan="3">Accion</th>
                        </tr>
                    </thead>
                    {{#each tiendas}}
                    <tr>
                        <td>{{this.nombre}}</td>
                        <!-- <td>{{this.sucursal}}</td> -->
                        <td>{{this.direccion}}</td>
                        <td>{{this.ciudad}}</td>
                        <td>{{this.region}}</td>
                        <td>{{this.region}}</td>
                        {{#if this.estado}}
                        <td><p class="btn btn-success" style="color: white;">Activado</p></td>
                        <td><a href="/control/eliminarTienda/{{this.id}}" class="btn btn-warning" style="color: red;">Eliminar</a></td>
                        {{else}}
                        <td><a href="/control/activarT/{{this.id}}" class="btn btn-warning" style="color: red;">Activar</a></td>
                        <td><a href="/control/eliminarTienda/{{this.id}}" class="btn btn-warning" style="color: red;">Eliminar</a></td>
                        {{/if}}
                    </tr>
                    {{/each}}
                </table>
                <a href="/control/agregarT" class="btn btn-primary w-100 mt-4">Agregar Tienda</a>
                <a href="/control/main" class="btn btn-danger w-100 mt-3">Volver</a>
            </div>
        </div>
    </div>
</div>